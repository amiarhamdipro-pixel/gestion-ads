# Meta (Phase 0 — voir meta-test.mjs)
META_ACCESS_TOKEN=
META_AD_ACCOUNT_ID=
META_API_VERSION=
META_CAMPAIGN_ID=
CAMPAIGN_NUMBER=
# Conversion perso "Lead - Confirmation de RDV" (voir BRIEF-CLAUDE-CODE.md section 5)
LEAD_ACTION_TYPE=

# Supabase (Phase 1)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Calendly (Phase 0 — voir scripts/test-calendly.ts)
CALENDLY_ACCESS_TOKEN=
CALENDLY_ORGANIZATION_URI=
# Optionnel : texte exact de la question "canal d'acquisition" du formulaire
# Calendly, si reformulée (défaut codé dans lib/calendly/appointments.ts).
CALENDLY_ACQUISITION_CHANNEL_QUESTION=

# URL publique du site — utilisée pour construire le lien de réinitialisation
# de mot de passe envoyé par Supabase Auth (lib/site.ts). En local, la valeur
# par défaut (http://localhost:3000) s'applique si cette variable est absente.
NEXT_PUBLIC_SITE_URL=https://ads.amerys-agency.com
