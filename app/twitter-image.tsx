// Aperçu Twitter/X : rigoureusement la même image que le partage générique
// (opengraph-image.tsx) — jamais une variante distincte, pour garantir une
// identité visuelle identique sur toutes les plateformes.
export { default, size, contentType } from './opengraph-image'
